#pragma once

/* QMK_KEYBOARD_H resolves to this file (keyboard short-name = rev1).
 * All keyboard-specific declarations live in mercurio.h; include it here
 * so that keymap.c gets everything via #include QMK_KEYBOARD_H.          */
#include "mercurio.h"
